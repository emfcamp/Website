import "core-js/stable/object/group-by";

import React from 'react';
import ReactDOM from 'react-dom';

import App from './schedule/App.jsx';

ReactDOM.render(React.createElement(App), document.getElementById('schedule-app'));
